# Ambiente / dependências
.env
__pycache__/
*.pyc
venv/
.venv/

# Sistema
.DS_Store
Thumbs.db

# Config local do Claude
.claude/settings.local.json

# Observação: data/db.json É versionado de propósito — é a base de teste
# (dados fictícios + estado inicial das etapas). Ao migrar para banco real
# no Render, passe a ignorá-lo.
